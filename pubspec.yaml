name: klinik_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  pdf: ^3.10.4
  printing: ^5.12.0
  barcode_widget: ^2.0.3
  fl_chart: ^0.69.0
  cupertino_icons: ^1.0.8
  barcode: ^2.2.3

  # Tambahan sesuai kebutuhan project
  dio: ^5.7.0
  shared_preferences: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Uncomment & tambah jika ingin menambahkan assets:
  # assets:
  #   - assets/images/